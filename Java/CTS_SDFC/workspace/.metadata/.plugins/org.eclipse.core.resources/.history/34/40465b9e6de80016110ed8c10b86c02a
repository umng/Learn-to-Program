import java.util.Comparator;

public class RevenueComparator implements Comparatoror<Revenue> {
	
	@Override
	public int compare(Revenue x, Revenue y) {
		if(x.getAmount() < y.getAmount())
			return x.getAmount();
		else
			return y.getAmount();
	}

}
